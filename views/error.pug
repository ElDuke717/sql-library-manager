extends layout

block content
  h1 Server Error
  p.form-text  Sorry!  There was an unexpected error on the server.

  h2.form-text  Error Details :
  h3= message
  h3= error.status
  pre #{error.stack}
